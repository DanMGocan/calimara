# Flask Secret Key (required for sessions and security)
SECRET_KEY='a_very_secret_key_replace_me'

# Base domain for the platform (e.g., calimara.ro)
BASE_DOMAIN='calimara.ro'

# Paths for database directories
# Ensure these directories exist and are writable by the application
# MAIN_DB_DIRECTORY='/path/to/your/main_db_directory' # Uncomment and set if not using default
# INSTANCE_DB_DIR_PATH='/path/to/your/instance_dbs' # Uncomment and set if not using default

# Stripe Configuration (replace with your actual keys)
STRIPE_PUBLISHABLE_KEY='pk_test_replace_me'
STRIPE_SECRET_KEY='sk_test_replace_me'
STRIPE_WEBHOOK_SECRET='whsec_replace_me'
# Replace 'price_12345' in platform_management/services.py with your actual Stripe Price ID

# Google reCAPTCHA Configuration (replace with your actual keys)
RECAPTCHA_PUBLIC_KEY='your_recaptcha_public_key'
RECAPTCHA_PRIVATE_KEY='your_recaptcha_private_key'

# Email Configuration (for Gmail - replace with your actual credentials)
# You might need to enable "Less secure app access" or use App Passwords in your Gmail settings
MAIL_SERVER='smtp.gmail.com'
MAIL_PORT=587
MAIL_USE_TLS='true'
MAIL_USERNAME='your_gmail_address@gmail.com'
MAIL_PASSWORD='your_gmail_password'
MAIL_DEFAULT_SENDER='Calimara Platform <noreply@calimara.ro>'

# Server Name for subdomain handling in development (optional)
# Set this to your base domain (e.g., 'localhost:5000') for local testing with subdomains
# You might also need to add entries to your hosts file (e.g., 127.0.0.1 test.calimara.ro)
# SERVER_NAME='localhost:5000' # Uncomment and set for local testing
